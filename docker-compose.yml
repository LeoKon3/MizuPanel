services:
  mizupanel:
    image: ${MIZUPANEL_IMAGE:-leokon3/mizupanel:0.1.24}
    container_name: ${MIZUPANEL_CONTAINER_NAME:-mizupanel}
    restart: unless-stopped
    ports:
      - "${MIZUPANEL_BIND_ADDR:-127.0.0.1}:${MIZUPANEL_PORT:-8080}:8080"
    environment:
      MIZUPANEL_AUDIT_RETENTION: ${MIZUPANEL_AUDIT_RETENTION:-90d}
      MIZUPANEL_AUDIT_CLEANUP_INTERVAL: ${MIZUPANEL_AUDIT_CLEANUP_INTERVAL:-1h}
      MIZUPANEL_TASK_RETENTION: ${MIZUPANEL_TASK_RETENTION:-30d}
      MIZUPANEL_TASK_CLEANUP_INTERVAL: ${MIZUPANEL_TASK_CLEANUP_INTERVAL:-1h}
    volumes:
      - ${MIZUPANEL_DATA_DIR:-./data}:/app/data
